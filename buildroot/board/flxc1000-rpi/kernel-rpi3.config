# RPi3 B+ specific kernel config fragment

# USB controller (dwc2 OTG built into BCM2837)
CONFIG_USB_DWC2=y

# Ethernet (LAN7515 USB hub+NIC on RPi3 B+)
CONFIG_USB_NET_DRIVERS=y
CONFIG_USB_USBNET=y
CONFIG_USB_LAN78XX=y

# SD card (BCM2835 SDHOST)
CONFIG_MMC_BCM2835=y
